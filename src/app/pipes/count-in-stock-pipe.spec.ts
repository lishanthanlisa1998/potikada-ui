import { CountInStockPipe } from './count-in-stock-pipe';

describe('CountInStockPipe', () => {
  it('create an instance', () => {
    const pipe = new CountInStockPipe();
    expect(pipe).toBeTruthy();
  });
});
